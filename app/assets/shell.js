(function () {
  "use strict";

  const config = window.SetfeedAppConfig;
  if (!config) throw new Error("Setfeed app configuration is missing.");
  const pages = [["dashboard","Home"],["send","Send"],["upcoming","Upcoming"],["awaiting","Awaiting"],["inbox","Inbox"],["sent","Sent"],["settings","Settings"]];

  function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);}
  function navigation(activePage,className){return `<nav class="${className}" aria-label="Setfeed app">${pages.map(([key,label])=>`<a href="${config.routes[key]}"${key===activePage?' aria-current="page"':""}>${escapeHtml(label)}</a>`).join("")}</nav>`;}
  function applyTheme(){const root=document.documentElement;let stored="system";try{stored=localStorage.getItem("sf_theme_mode")||"system";}catch(_){}if(stored==="light"||stored==="dark")root.setAttribute("data-theme",stored);else root.removeAttribute("data-theme");}
  function loadScript(src){return new Promise((resolve,reject)=>{const existing=document.querySelector(`script[src="${src}"]`);if(existing){if(existing.dataset.loaded==="1")resolve();else existing.addEventListener("load",resolve,{once:true});return;}const script=document.createElement("script");script.src=src;script.onload=()=>{script.dataset.loaded="1";resolve();};script.onerror=reject;document.head.append(script);});}
  async function startAuth(){try{if(!window.firebase)await loadScript("https://www.gstatic.com/firebasejs/10.12.2/firebase-app-compat.js");if(!window.firebase||!firebase.auth)await loadScript("https://www.gstatic.com/firebasejs/10.12.2/firebase-auth-compat.js");if(!window.SetfeedDiscordAuth)await loadScript("../assets/discord-auth.js");if(!window.SetfeedJsonApiClient)await loadScript("./assets/api.js");if(!window.SetfeedAppAuth)await loadScript("./assets/auth-core.js");await loadScript("./assets/auth-ui.js");const page=document.body.dataset.appPage;if(page==="send"){await loadScript("./assets/recipients.js");await loadScript("./assets/compose-core.js");await loadScript("./assets/compose.js");}if(page==="upcoming"||page==="awaiting"){await loadScript("./assets/message-lists-core.js");await loadScript("./assets/message-lists-data.js");await loadScript("./assets/message-lists-ui.js");}}catch(_){const status=document.getElementById("app-auth-status");if(status)status.textContent="Sign-in unavailable";}}
  function render(){applyTheme();const page=document.body.dataset.appPage||"dashboard";const title=document.body.dataset.appTitle||"Setfeed";const description=document.body.dataset.appDescription||"Manage your Setfeed account.";const content=document.getElementById("app-page-content");const root=document.getElementById("setfeed-app");if(!content||!root)return;root.innerHTML=`<header class="site-header"><div class="header-inner"><a class="brand-mark" href="${config.routes.websiteHome}" aria-label="Setfeed home"><img class="logo" src="../logo.png" alt="" aria-hidden="true"></a><a class="brand-title" href="${config.routes.dashboard}">Setfeed</a><a class="btn btn-quiet" href="${config.routes.websiteHome}">Website</a></div></header><main class="wrap">${navigation(page,"app-mobile-nav")}<div class="app-layout"><aside class="app-sidebar">${navigation(page,"app-nav")}</aside><section class="app-main" aria-labelledby="app-page-title"><div class="app-topbar"><div><p class="app-kicker">Setfeed web app</p><h1 class="app-title" id="app-page-title">${escapeHtml(title)}</h1><p class="lead">${escapeHtml(description)}</p></div><span class="app-status" id="app-auth-status">Checking sign-in…</span></div><div id="app-content-slot"></div></section></div></main>`;const slot=document.getElementById("app-content-slot");if(slot)slot.append(content);startAuth();}
  window.SetfeedAppShell={render};
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",render,{once:true});else render();
})();